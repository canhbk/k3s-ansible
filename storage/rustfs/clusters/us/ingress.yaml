# RustFS S3 API Ingress for US Cluster
# Access via: https://rustfs.us.canhnv.com
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rustfs-api
  namespace: rustfs
  annotations:
    cert-manager.io/cluster-issuer: "canhnv-com-prod"
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - rustfs.us.canhnv.com
      secretName: rustfs.us.canhnv.com-tls
  rules:
    - host: rustfs.us.canhnv.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rustfs-svc
                port:
                  number: 9000
---
# RustFS Web Console Ingress for US Cluster
# Access via: https://rustfs-console.us.canhnv.com
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rustfs-console
  namespace: rustfs
  annotations:
    cert-manager.io/cluster-issuer: "canhnv-com-prod"
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - rustfs-console.us.canhnv.com
      secretName: rustfs-console.us.canhnv.com-tls
  rules:
    - host: rustfs-console.us.canhnv.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rustfs-svc
                port:
                  number: 9001
