apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: pact-broker-base

namespace: pact-broker

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - secret.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: pact-broker
