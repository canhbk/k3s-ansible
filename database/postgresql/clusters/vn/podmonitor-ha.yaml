# PodMonitor for PostgreSQL HA cluster metrics
# This configures Prometheus to scrape CNPG PostgreSQL pod metrics
#
# Apply: kubectl apply -f podmonitor-ha.yaml

apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgresql-ha
  namespace: postgres-db
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: postgresql-ha
  podMetricsEndpoints:
  - port: metrics
    interval: 30s
    scrapeTimeout: 10s
    path: /metrics
