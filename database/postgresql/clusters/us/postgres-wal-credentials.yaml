# PostgreSQL WAL Archiving Credentials for US Cluster
# Generated on: 2025-08-27
# IMPORTANT: This file contains sensitive information - do not commit to git

apiVersion: v1
kind: Secret
metadata:
  name: postgres-wal-credentials
  namespace: postgres-system
type: Opaque
stringData:
  # MinIO Configuration
  MINIO_ENDPOINT: "http://64.71.161.44:9000"
  BUCKET_NAME: "postgres-wal"
  
  # MinIO PostgreSQL WAL User Credentials
  POSTGRES_WAL_USER: "postgres-user"
  POSTGRES_WAL_PASSWORD: "__REDACTED__"
  
  # Connection string format for CloudNativePG
  # s3://bucket-name?endpoint=http://64.71.161.44:9000&access-key-id=postgres-user&secret-access-key=__REDACTED__&force-path-style=true
  BARMAN_S3_CONNECTION: "s3://postgres-wal?endpoint=http://64.71.161.44:9000&access-key-id=postgres-user&secret-access-key=__REDACTED__&force-path-style=true"

---
# Additional backup configuration details
# Use these values when configuring CloudNativePG cluster:
#
# backup:
#   target: "primary"
#   retentionPolicy: "30d"
#   barmanObjectStore:
#     destinationPath: s3://postgres-wal
#     s3Credentials:
#       accessKeyId:
#         name: postgres-wal-credentials
#         key: POSTGRES_WAL_USER
#       secretAccessKey:
#         name: postgres-wal-credentials
#         key: POSTGRES_WAL_PASSWORD
#     endpointURL: http://64.71.161.44:9000
#     # Use s3ForcePathStyle: true for MinIO compatibility
#     s3ForcePathStyle: true