# Base values for kube-prometheus-stack
# These values are shared across all clusters
# Cluster-specific values should override these

# Global settings
global:
  evaluation_interval: 30s
  scrape_interval: 30s
  scrape_timeout: 10s

# Prometheus Operator
prometheusOperator:
  resources:
    limits:
      memory: 400Mi
    requests:
      cpu: 100m
      memory: 200Mi

# Prometheus configuration
prometheus:
  prometheusSpec:
    # Retention settings - 30 days
    retention: 30d
    retentionSize: "50GB"

    # Resource limits
    resources:
      requests:
        cpu: 500m
        memory: 2Gi
      limits:
        memory: 4Gi

    # Storage configuration
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: local-path
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 60Gi

    # Service discovery
    serviceMonitorSelector:
      matchLabels:
        prometheus: kube-prometheus-stack  # This label is required for Prometheus to pick it up
    serviceMonitorNamespaceSelector: {}
    podMonitorSelector: {}
    podMonitorNamespaceSelector: {}

    # ScrapeConfig selector - allows selecting ScrapeConfig resources from any namespace
    # This enables creating ScrapeConfig CRs in other namespaces that will be picked up by Prometheus
    scrapeConfigSelector:
      matchLabels:
        prometheus: kube-prometheus-stack
    # Example which selects scrapeConfigs with label "prometheus" set to "kube-prometheus-stack"
    # scrapeConfigSelector:
    #   matchLabels:
    #     prometheus: kube-prometheus-stack

    # Namespace selector for ScrapeConfig resources
    # Leave empty to select from all namespaces
    scrapeConfigNamespaceSelector: {}

    # Enable features
    enableFeatures:
      - memory-snapshot-on-shutdown
      - new-service-discovery-manager

    # Additional scrape configs for K3s components
    # Note: K3s embeds controller-manager and scheduler in the k3s server process
    # These components don't expose metrics on standard ports like in vanilla K8s
    # Commenting out for now as kubelet metrics are already collected via ServiceMonitor
    # additionalScrapeConfigs:
    # - job_name: 'k3s-components'
    #   static_configs:
    #   - targets:
    #     - '154.26.131.23:10250'  # kubelet on control plane
    #     - '154.26.131.23:10257'  # kube-controller-manager (not available in K3s)
    #     - '154.26.131.23:10259'  # kube-scheduler (not available in K3s)

# Grafana configuration
grafana:
  # Admin credentials will be generated and stored in secret
  adminUser: admin

  # Persistence
  persistence:
    enabled: true
    size: 10Gi
    storageClassName: local-path

  # Resources
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      memory: 512Mi

  # Default dashboards
  defaultDashboardsEnabled: true
  defaultDashboardsTimezone: utc

  # Grafana.ini configuration
  grafana.ini:
    server:
      root_url: "%(protocol)s://%(domain)s/"
    auth:
      disable_login_form: false
    users:
      auto_assign_org_role: Editor
    analytics:
      reporting_enabled: false
      check_for_updates: false

  # Sidecar for dashboard/datasource provisioning
  sidecar:
    dashboards:
      enabled: true
      searchNamespace: ALL
      provider:
        disableDelete: false
        allowUiUpdates: true
    datasources:
      enabled: true
      defaultDatasourceEnabled: true

  # Pre-install dashboards via ConfigMaps
  dashboardProviders:
    dashboardproviders.yaml:
      apiVersion: 1
      providers:
      - name: 'default'
        orgId: 1
        folder: ''
        type: file
        disableDeletion: false
        updateIntervalSeconds: 10
        editable: true
        options:
          path: /var/lib/grafana/dashboards/default
      - name: 'k3s'
        orgId: 1
        folder: 'K3s'
        type: file
        disableDeletion: false
        updateIntervalSeconds: 10
        editable: true
        options:
          path: /var/lib/grafana/dashboards/k3s

# Alertmanager configuration
alertmanager:
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: local-path
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        memory: 128Mi

# Node exporter
nodeExporter:
  resources:
    requests:
      cpu: 50m
      memory: 32Mi
    limits:
      memory: 64Mi

# Kube-state-metrics
kubeStateMetrics:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      memory: 128Mi

# Disable components we don't need
kubeApiServer:
  enabled: true
kubeControllerManager:
  enabled: false  # K3s doesn't expose controller-manager metrics by default
kubeScheduler:
  enabled: false  # K3s doesn't expose scheduler metrics by default
kubeProxy:
  enabled: false  # K3s uses different proxy implementation
kubeEtcd:
  enabled: false  # K3s uses embedded etcd

# ServiceMonitors for K3s
defaultRules:
  create: true
  rules:
    kubeProxy: false  # Disable kube-proxy rules for K3s

# Additional labels for multi-cluster identification
commonLabels:
  prometheus: kube-prometheus
