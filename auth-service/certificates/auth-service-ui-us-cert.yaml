apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-ambercare-app-tls
  namespace: murror-platform
spec:
  secretName: auth-ambercare-app-tls
  issuerRef:
    name: ambercare-app
    kind: ClusterIssuer
  dnsNames:
    - auth.ambercare.app
