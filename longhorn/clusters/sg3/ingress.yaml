---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: longhorn-auth
  namespace: longhorn-system
spec:
  basicAuth:
    secret: basic-auth
    realm: "Authentication Required"
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-tls-cert
  namespace: longhorn-system
spec:
  secretName: longhorn-tls
  issuerRef:
    name: canhnv-com-prod
    kind: ClusterIssuer
  commonName: "sg3.longhorn.canhnv.com"
  dnsNames:
    - "sg3.longhorn.canhnv.com"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-ingress
  namespace: longhorn-system
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: longhorn-system-longhorn-auth@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - sg3.longhorn.canhnv.com
    secretName: longhorn-tls
  rules:
  - host: sg3.longhorn.canhnv.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: longhorn-frontend
            port:
              number: 80
