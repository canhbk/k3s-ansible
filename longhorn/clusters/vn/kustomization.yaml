apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
- ingress.yaml

patchesStrategicMerge:
- values.yaml

configMapGenerator:
- name: longhorn-vn-config
  files:
  - values.yaml